from config.bd import bd, app, ma

class estudiante(bd.Model):
    __tablename__ ='Estudiante'
    usuario = bd.Column(bd.Integer, primary_key = True)
    contraseña = bd.Column(bd.String(50))
    entrada = bd.Column(bd.String(50))
    salida = bd.Column(bd.String(50))


    def __init__(self, usuario,contraseña):
        self.namusuarioetak = usuario
        self.contraseña = contraseña


with app.app_context():
    bd.create_all()

class estudianteSchema(ma.Schema):
    class Meta:
        fields =( 'usuario', 'contraseña')